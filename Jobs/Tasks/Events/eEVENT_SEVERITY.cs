namespace Jobs.Tasks.Events
{
    /// <summary>
    /// Indicates if a problem was handled by the task or not.
    /// </summary>
    public enum eEVENT_SEVERITY
    {
        HANDLED,
        UNHANDLED
    }
}